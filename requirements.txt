# Install torch FIRST, matched to your GPU architecture, then this file.
# Blackwell (sm_120) needs a CUDA 12.8+ build:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# Verify your arch is covered before training:
#   python -c "import torch; print(torch.cuda.get_arch_list())"
torch>=2.4
torchvision>=0.19

timm>=1.0.11              # pretrained encoders (ConvNeXt, Swin)
albumentations>=2.0       # augmentation; 2.x API is required by src/datasets/transforms.py
opencv-python-headless>=4.9
numpy>=1.26
pandas>=2.0
scikit-learn>=1.4
scipy>=1.11
PyYAML>=6.0
matplotlib>=3.8           # confusion-matrix figures
tensorboard>=2.16         # optional: set logging.tensorboard=false to skip
# wandb                   # optional: pip install wandb, then --set logging.wandb=true
